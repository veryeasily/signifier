
$(function() {
  /*
  	# getColor = ->
  		a = (Math.floor(Math.random() * 16)).toString(16)
  		b = (Math.floor(Math.random() * 16)).toString(16)
  		a + b
  
  	rand = Math.floor(Math.random() * 20) + 1
  	# $css = $("<style>").attr("type", "text/css")
  	for a in [0...rand]
  		$thing = $("<div>").css(
  			position: "absolute"
  			left: Math.floor(Math.random() * 1100) + "px"
  			top: Math.floor(Math.random() * 500) + "px"
  		)
  		inty = Math.floor(Math.random() * 9) + 1
  		obj = document.createElement("object")
  		obj.data = "svg/" + inty + ".svg"
  		obj.type = "image/svg+xml"
  		$(obj).addClass("rand" + a)
  		tempy = "#" + getColor() + getColor() + getColor()
  		# $css.html($css.html() + "\n.rand" + i + " svg {stroke: " + tempy + "; fill: " + tempy + ";}")
  		$thing[0].appendChild(obj)
  		document.body.appendChild($thing[0])
  		$(obj).children().children("svg").attr("stroke", tempy).attr("fill", tempy)
  */
});
